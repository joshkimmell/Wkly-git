{
  "name": "weekly-goals",
  "private": true,
  "scripts": {
    "dev": "netlify dev --prefix frontend",
    "build": "vite build --prefix frontend",
    "preview": "vite preview --prefix frontend"
  },
  "dependencies": {
    "@netlify/functions": "^4.1.5"
  }
}
